# Download with Streamlit and Golang

# Setup app and venv
```sh
python3 -m venv venv
source ./venv/bin/activate
pip install -r requirementx.txt
streamlit run main.py
```

# Setup Golang service
```sh
go mod init
go run main.go
```